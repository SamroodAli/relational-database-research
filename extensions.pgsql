-- We can install and use extensions in databases as follows
-- Extensions extend default and built-in functionalities, adding new features.

CREATE EXTENSION [IF NOT EXISTS] extension_name;

-- Example

CREATE EXTENSION cube;
CREATE EXTENSION earthdistance;
CREATE EXTENSION intarray;
