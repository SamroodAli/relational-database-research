# Apart from dropping/deleting DROP database_name query, we can also use the 
# dropdb utility

dropdb database_name;

#unlike dropdb, calling dropdb without any arguments will just error out.
# dropdb requires a database name