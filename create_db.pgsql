-- Create a new database called 'DatabaseName'
CREATE DATABASE database_name; -- underscores are allowed but something like colons will require quotes


CREATE DATABASE "database-name" -- requires quotes as this name has colons. 

-- I guess I can stick to underscores then : )